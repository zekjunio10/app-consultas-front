// api_config.js
// Configuração do link da API
window.API_BASE_URL = "https://api-consultasbrasil.zekjunio10.workers.dev/routes";
window.HOME_BASE_URL = "https://api-consultas-b7v.pages.dev";
/*// Caminho interno (usado enquanto não há .htaccess)
const API_ROUTES_PATH = "/routes"; 

// Helper simples para montar URLs da API
function apiUrl(endpoint) {
    return `${API_BASE_URL}${API_ROUTES_PATH}${endpoint}`;
}
*/